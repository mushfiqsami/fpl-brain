data/*
# except the seeded prior: build_standalone.py embeds it, so a clone that
# lacks it produces a bundle with no player baselines at all.
!data/player_prior.json
__pycache__/
*.pyc

# Scratch copies of the modules unpacked out of app.py's base64 blobs.
# See CLAUDE.md - app.py is the artifact, these are a working area.
decoded/
page.html
page_js.js
